/**
 * Prettier configuration for clean code formatting and spacing.
 * @type {import('prettier').Config} The configuration type.
 * @const
 */
export default {
    arrowParens: 'always',
    tabWidth: 3,
    useTabs: false,
    semi: false,
    singleQuote: true,
    quoteProps: 'as-needed',
    trailingComma: 'none',
    bracketSpacing: true
 }